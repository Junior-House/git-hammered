import tensorflow as tf
import numpy as np

class HammeredModel:
    
    self def train(dataset, learningRate = 0.1, numIterations = 1000, batchSize = 100, displayStep = 2):
        print("Gooday mate")
        return

    # returns true if the audio sample is drunk, returns false if the sample is sober
    self def inference(audioSample) -> Boolean:
        print("He's drunk as a skunk mate")
        return True

    